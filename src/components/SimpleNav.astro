---
import './SimpleNav.css';
import { simpleNavLinks } from '../data/navLinks';
---

<div class="mx-auto space-x-6">
  <div class="simplenavlinks">
    {simpleNavLinks.map(({ href, label }) => (
      <a href={href} class="simplenavlink">{label}</a>
    ))}
  </div>
</div>
